{
  "name": "@parad0x_labs/openclaw-context-capsule",
  "version": "1.6.0",
  "description": "Self-contained OpenClaw context engine that compresses older agent session history into a bounded extractive capsule, preserving decisions, tasks, errors, files, links, and durable facts while keeping recent messages verbatim.",
  "type": "module",
  "main": "./dist/index.js",
  "files": [
    "dist/",
    "openclaw.plugin.json",
    "README.md",
    "SKILL.md"
  ],
  "keywords": [
    "openclaw",
    "openclaw-plugin",
    "context-compression",
    "llm",
    "token-savings",
    "ollama",
    "context-capsule"
  ],
  "openclaw": {
    "extensions": [
      "./dist/index.js"
    ],
    "build": {
      "openclawVersion": ">=2026.6.0"
    }
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "prepack": "npm run build",
    "prepublishOnly": "npm run build && npm test",
    "test": "npm run build && node test/compression-smoke.mjs && node test/quality.test.mjs && node test/supersession-bench.mjs && node test/hardening-bench.mjs && node test/secret-bench.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/Parad0x-Labs/openclaw-skills",
    "directory": "skills/context-capsule"
  }
}
