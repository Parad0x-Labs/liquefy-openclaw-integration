# Never execute third-party install lifecycle scripts in this repo. The skills'
# deps are pure JS (native optionals fall back), so this loses nothing and closes
# the supply-chain keyscanner vector on any host that holds the npm token or keys.
ignore-scripts=true
