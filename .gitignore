# Parad0x Labs - Liquefy .gitignore

# Internal Audits & Sensitive Docs (Scoped to prevent "audit smell")
INTERNAL_AUDIT/**/*.txt
INTERNAL_AUDIT/**/*.log
REPORTS/INTERNAL_REVIEW_*
MISSION_LOG_*
INTERNAL_REVIEW_*
CURSOR_FIXED_HANDOVER*
CURSOR_SUGGESTED_GIT_TREE*
BUILD.md

# Social media drafts / promo posts (never publish to repo)
*DISCORD_POST*
*X_POST*
*LAUNCH_POST*
*_POST.md
*_TWEET*

# Scaffolded agent workspaces (user-generated, contain local paths)
/agents/

# Local analysis / one-off reports (never publish)
api/CORRECTED_PERFORMANCE_REPORT.md
api/FINAL_SPEED_COMPRESSION_REPORT.md
api/MRTV_PROTECTION_VALUE_REPORT.md
api/NULL_Suite_V1_Certification.md
api/PARAD0X_LABS_MASTER_CODEX.txt
api/accurate_performance_benchmark.py
api/comprehensive_engine_analysis.py
api/final_status_check.py
api/verify_performance.py

# Python
__pycache__/
*.py[cod]
*$py.class
venv/
.venv/
env/

# Docker / Build
appliance-public/decoder_source.py
dist/
!plugins/openclaw-plugin/dist/
!plugins/openclaw-plugin/dist/*.js
build/
*.spec

# OS
.DS_Store
Thumbs.db

# Test / Build artifacts
.pytest_cache/
*.null
*.gz
.env
logs/
out/
api/out/
vault/
restored/

# Local uploads / generated artifacts (never publish)
api/uploads/
api/**/uploads/

# Benchmark datasets / generated results / vendored binaries
bench/datasets/
bench/results/
bench/bin/

# Benchmark runner local outputs
benchmarks/latest_ci_subset.csv

# Local archives / bundles
*.zst
*.tar
*.zip

# Keep test fixtures and docs images tracked (do not blanket-ignore *.log/*.jsonl/*.png)
tests/redteam/tmp/
